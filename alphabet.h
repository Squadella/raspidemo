#ifndef LIBAL
#define LIBAL

void drawAlphabet(char letter, int resize, int *image, Pixel center, int width, int max);
void drawQuarterCircle(const Pixel center, int radius, int *image, int height, int width, int quarter);

#endif